// Write a function that multiples two
//  numbers. Call that function to multiply any two numbers of
//  your choice and log the answer in your console

function multiplyNumbers(x, y) {
  return x * y;
}
const result = multiplyNumbers(4, 5);

console.log(result);
console.log(multiplyNumbers(8, 9));

// create variables for your first name, last name,
// Stutern track, skills, favourite color,
// year of your country’s independence.
// Use these variables to form a sentence and log the sentence in the console.

const firstName = "Angela";
const lastName = "Raphael";

const Stutern_track = "Frontend web development";
const skill = "coding";
const favouriteColor = "white";
country_Independence = 1960;

const sentence = `My name is ${firstName} ${lastName} and I am undergoing a bootcamp with Stutern in the ${Stutern_track} track. One of my skills among many others is ${skill}. My best colour is ${favouriteColor}. Fun fact: My country was granted full independence on 1st October ${country_Independence}`;

console.log(sentence);
